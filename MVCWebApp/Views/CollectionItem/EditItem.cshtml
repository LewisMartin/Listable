@model Listable.MVCWebApp.ViewModels.Collections.EditItemViewModel

@{
    ViewData["Title"] = "CreateItem";
}

<div class="listable-collections-container">
    <div class="listable-collections-title">
        @Model.CollectionName > Edit Item
    </div>
    <input asp-for="ItemId" hidden />
    @Html.EditorFor(m => m.ItemDetails)
</div>

